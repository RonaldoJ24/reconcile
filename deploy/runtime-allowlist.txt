# Runtime image boundary for Reconcile ML v1.
backend/src/reconcile/
artifacts/ranker-ml-v1/model.pkl
artifacts/ranker-ml-v1/metadata.json
deploy/runtime-allowlist.txt
pyproject.toml
uv.lock

# Deliberately excluded: generated inputs/targets/manifests, reports, notebooks,
# caches, and training utilities.
